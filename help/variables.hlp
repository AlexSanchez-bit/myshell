las variables son almacenadas en dos listas como pares llave-valor , los comandos que contienen las ``
al hacer get a sus variables , ejecutan el comano almacenado en el mismo pipeline
que un comando normal ejecutado por la shell , es decir que tiene los mismos
comandos y funcionalidades

comandos builtin
set <var> <value> asigna o reasigna la variable var con un valor value
get <var> permite obtener el resultado almacenado en var
unset <var> permite eliminar valores de la lista
